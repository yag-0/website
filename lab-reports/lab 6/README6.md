# Лабораторна робота 6

    **Студент:** Кучерук Максим
    **Група:** Соло

    ## 📋 Опис проєкту

    Інтернет магазин техніки в мінімалістичному стилі.Містить такі сторінки та відповідні до цих сторінок стандартні функції:Магазин, Кошик, Головна, Про нас, Зворотній зв'язок, Адмін та api-demo


    ## 📁 Структура проєкту

    ```
    lab03-flaskProject/
    ├── app.py                  # Основний Flask застосунок
    ├── models.py               # Функції для роботи з БД та API
    ├── api.py                  # REST API ендпоінти (Blueprint)
    ├── requirements.txt        # Залежності (Flask, flasgger, Flask-Cors)
    ├── site.db                 # SQLite база даних
    ├── templates/
    │   ├── base.html           # Шаблон з навігацією
    │   ├── home.html           # Головна сторінка
    │   ├── market.html         # Магазин з товарами
    │   ├── cart.html           # Кошик
    │   ├── about.html          # Про нас
    │   ├── HELP.html           # Зворотний зв'язок
    │   ├── api-demo.html       # Frontend інтеграція з API
    │   └── admin/              # Адмін-панель
    │       ├── index.html
    │       ├── login.html
    │       ├── products.html
    │       ├── orders.html
    │       ├── customers.html
    │       ├── promo.html
    │       └── feedback.html
    ├── static/                 # Статичні файли
    └── run_server.bat          # Запуск сервера
    ```

    ## 🔌 API Endpoints

    Реалізовано REST API з 9 ендпоїнтами для управління товарами, замовленнями та відгуками:

    ### GET /api/products
    Отримує список всіх товарів.

    **Відповідь:**
    ```json
    [
      {
        "id": 1,
        "name": "Ноутбук",
        "price": 25000.50,
        "stock": 10,
        "category": "техніка",
        "description": "Потужний ноутбук"
      }
    ]
    ```

    ### GET /api/orders
    Отримує список всіх замовлень.

    **Відповідь:**
    ```json
    [
      {
        "id": 1,
        "customer_name": "Іван Петренко",
        "customer_email": "ivan@example.com",
        "status": "new",
        "created_at": "2024-12-07T10:30:00",
        "promo_code": "1234",
        "discount_amount": 250.50
      }
    ]
    ```

    ### POST /api/feedback
    Створює новий відгук.

    **Тіло запиту:**
    ```json
    {
      "name": "Іван",
      "email": "ivan@example.com",
      "message": "Відмінна якість!"
    }
    ```

    ### DELETE /api/feedback/{id}
    Видаляє відгук за ID.

    **Відповідь:**
    ```json
    {
      "message": "Feedback deleted successfully"
    }
    ```

    ### PUT /api/orders/{id}
    Оновлює статус замовлення.

    **Тіло запиту:**
    ```json
    {
      "status": "processing"
    }
    ```

    Інші ендпоінти: `GET /api/orders/{id}`, `POST /api/orders`, `GET /api/feedback`.
    Повна документація доступна на `/apidocs` (Swagger UI).


    ## 📸 Скріншоти

    ### Головна сторінка
    ![Скріншот головної сторінки](../lab6/mainapi.png)

    ### Додавання запису
    ![Скріншот форми](../lab6/add.png)

    ### Повідомлення про успіх
    ![Скріншот повідомлення](../lab6/success.png)

    ## 🔗 Посилання
    - [Посилання на GitHub](https://github.com/yag-0/website.git)

    ## ✅ Висновки

    У цій лабораторній роботі я створив інтерактивну сторінку api-demo, яка демонструє інтеграцію frontend з backend API, включаючи завантаження даних, додавання нових записів та видалення. Застосував Flask-Cors для дозволу кроссдоменних запитів та Swagger (flasgger) для автоматичної генерації документації API. Навчився писати охайний та модульний код з використанням Blueprint для організації маршрутів.